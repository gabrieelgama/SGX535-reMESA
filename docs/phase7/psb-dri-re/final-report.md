# Static psb_dri.so investigation result

The analyzed artifact is the existing `xpsb-glx` 0.18-era `dri/psb_dri.so`, SHA-256 `74ca42991906741ee91be1bc088ae0b142fa71b6a85658c5dad0851ce50dd0d8` (P7B-001/P7B-014). It was parsed and decompiled as data only. The original Phase 7.0–7.5 evidence and gates remain in force.

| subject | result |
|---|---|
| Historical DRI ABI | Eight version-1 descriptors and 23 non-null callback fields mapped against Mesa 7.4.4. All 23 callback bodies were decompiled in the disposable project; driver-specific screen list assignment is confirmed. The DRI2 screen constructor's fixed `InitScreen2` slot is NULL in this build (P7B-002/P7B-003/P7B-011). |
| Function map | 51 address-stable entries: 23 ABI callbacks plus 28 reviewed driver/SGX paths. Original Ghidra names and ELF addresses are retained. |
| Kernel boundary | Two direct libdrm command requests: index 3 / 0x14 bytes and index 0 / 0x90 bytes. Historical PSB header names `SCENE_UNREF` and `CMDBUF` match by number/size, but exact paired ABI version is INFERRED (P7B-004). Other BO/fence requests are delegated through `libdrm.so.2` (P7B-006). |
| Submission | A DRI swap callback reaches a 2D copy command builder and the direct submit wrapper (P7B-005). A separate scene-finalization path emits TA/PDS-related state and reaches the same wrapper with 3D engine selection (P7B-012). **COMMAND SUBMISSION: STATICALLY RECONSTRUCTED** for these bounded paths; no command was executed. At this P7B checkpoint the indirect draw-to-finalize transition was UNKNOWN; P7E later resolved it in [the draw trace](draw-to-submit.md). |
| Buffers and sync | BO create/map, outbuf reservation, relocation construction, fence wait and conditional lock handling are located (P7B-006/P7B-007). Exact paired libdrm/kernel semantics remain UNKNOWN. |
| SGX programs | The ELF contains USC/USSE output-generation code and a vertex path with separate USE/USSE instruction slots and PDS-related output (P7B-008/P7B-009; buffer-label correction P7F-003). Full instruction semantics, target SGX core revision and architectural behavior are UNKNOWN. No static microkernel or GPU program blob has been confirmed by a bounded XREF/upload chain. |
| External components | Six direct DT_NEEDED libraries, including `libdrm.so.2`, and no direct named Services dependency (P7B-010). The same package contains `Xpsb.so`; the exact paired X-server/kernel/libdrm stack is not established. |

The historical binary substantially constrains U17–U20, as tracked in [unknowns](unknowns.md), but it does not identify the Dell's physical SGX revision, restore missing firmware scripts, prove `CORE_ID`/`CORE_REVISION` read safety or change ownership/power/failure gates. FIRST-OBSERVATION-DESIGN and Gate B remain **BLOCKED**, both registers remain **not SAFE-CANDIDATE**, and the whitelist remains `[]`.

The narrowest next static step is to analyze the already recovered `Xpsb.so` and correlate its `PsbDRIRec` producer with this ELF's 0x20-byte record check, then identify the exact paired PSB kernel and libdrm ABI. That would test whether the two sides of the historical userspace contract can be specified without copying the proprietary implementation. Hardware bring-up still requires the separate SGX register/power/clock/failure contract.

**Follow-up:** [The Xpsb/libdrm investigation](../xpsb-re/README.md) performed that static next step. It found the `PsbDRIRec` producer in a public Xorg DDX source release, not inside `Xpsb.so`; Xpsb takes a separate BAR0 mapping and DRM fd. It recovered candidate libdrm ioctl definitions and XHW/command paths, but could not authenticate an exact paired DDX/libdrm/kernel build. The historical software map has expanded; hardware gates and this binary's original P7B conclusions remain unchanged.

**Later P7D static pass:** The public PSB kernel source `4.41.1` embeds the same `5.0.1.0046` package label as both retained ELFs and has matching direct-request sizes, strengthening the historical release-family correlation without proving an exact built stack. [The DRI draw-path update](command-submission.md) showed context creation installing draw and scene-finalization callbacks together; P7E subsequently resolved their indirect mode-transition connection while frontend invocation still remains open. [The Xpsb buffer trace](../xpsb-re/static-buffer.md) establishes a separate X-server composite-template path; it does not substitute for the DRI GL path.

**Behavior reconstructed:** selected historical DRI, BO/relocation, 2D and scene/TA submit paths. **Implementation possible:** an architectural clean design can be scoped, but a complete interoperable minimal 3D implementation is not yet specified. **Implementation written:** none. **Implementation compiled:** none. **HARDWARE FUNCTIONALITY: UNVERIFIED.** No MMIO, ioctl, firmware execution or workload was performed.
# P7E follow-up: final pre-implementation pass

[The indirect control-flow trace](draw-to-submit.md) resolves the historical mode-3-exit → renderer finalizer → scene submit edge missed by a direct-call-only graph. It establishes conditional submit triggers, not an unconditional per-draw submission rule. The private callback's frontend GL invocation and complete dirty-state sequence remain open. [The format inventory](format-readiness.md) and [readiness matrix](clean-room-readiness.md) give the current strict result: **MINIMAL CLEAN-ROOM 3D USERSPACE PATH NOT YET SPECIFIABLE**. The report above remains the P7B checkpoint; P7E adds evidence rather than replacing it. Gate B remains BLOCKED, whitelist `[]`, hardware functionality UNVERIFIED.

## P7F bounded-path follow-up

[One conditional eight-position setup branch](bounded-path-closure.md) now has a 128-byte symbolic CPU vertex layout, six known 16-bit indices, two derived eight-byte USE/USSE instruction slots, selected TA index literals and a ten-word conditional general-state record. This closes literal bytes for **that branch only**, not a valid minimal GL draw. [The interface/bootstrap trace](validation-bootstrap-closure.md) tightens the validation-node, command, candidate kernel fence and Xpsb initialization evidence. P7F-003 explicitly corrects the earlier mislabeling of the `0x00030ffd` USE instruction buffer as PDS; the separate PDS-related vertex output remains.

The [strict pre-implementation decision](preimplementation-decision.md) remains **Result B**. P7F-010 identifies a dirty-state atom containing the renderer selector and a matching indirect dispatcher; context creation installs that atom list (P7F-011); the exact GL draw entry remains open. Scene dirty-state coverage, other required programs, vertex-output holes/relocation values, exact bootstrap/ABI pairing, synchronization and SGX535 architectural semantics are incomplete. No driver was written or executed; Gate B is BLOCKED and hardware functionality is UNVERIFIED.


The latest [P7G frozen-draw result](frozen-draw-closure.md) supersedes the earlier vertex-count interpretation and traces the historical frontend entry. Its [blocker table](frozen-draw-blockers.md) retains Result B and names static work still available.
