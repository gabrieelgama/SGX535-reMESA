# Unknown reduction from the xpsb-glx ELF

This is a static update to the existing [project unknown register](../../unknowns.md). An ELF finding can confirm what historical userspace bytes do; it cannot establish the Dell's SGX power state or authorize hardware access.

| existing ID | result from this artifact | classification change |
|---|---|---|
| U01–U06, U10–U14 | No physical core revision, read-safety, clock, lock, CPU failure or recovery contract appears in the analyzed paths | still UNKNOWN |
| U07–U09, U12 | Userspace constructs buffer/relocation addresses, but no new explanation of Linux/DDK BIF directory-list divergence or GTT-to-BIF physical path | still UNKNOWN |
| U15 | DRI screen/context init is located; matching Poulsbo Services init scripts and DDK `DevInitSGXPart2KM` producer are not | partially constrained; scripts UNKNOWN |
| U16 | No proven Poulsbo microkernel/firmware artifact or licensable payload identified | still UNKNOWN |
| U17 | Userspace vertex/PDS-related program generation and upload paths are located, but exact bootstrap program and execution protocol are not | partially constrained; bootstrap UNKNOWN |
| U18 | Historical direct request indices `0`/`3`, sizes `0x90`/`0x14`, BO/fence imports, validation/relocation construction and 2D and scene/TA submit paths are now directly located | partially constrained; full 3D/CCB/sync ABI UNKNOWN |
| U19 | Embedded USC/USSE generation and PDS-related assembly paths are confirmed in this proprietary binary | previously “encoder absent” is superseded for historical binary presence; complete open ISA/encoder specification and revision applicability UNKNOWN |
| U20 | Scene, TA, shader and state modules are located through assertions/XREFs; full packet and render contract is not reconstructed | partially constrained; rendering UNKNOWN |

New binary-specific questions:

| ID | question | evidence needed |
|---|---|---|
| P7B-U01 | Which exact historical `libdrm.so.2`, Xorg DDX and PSB kernel were paired with this ELF and its same-archive `Xpsb.so`? | authenticated matching package versions and ABI comparison; see [Xpsb follow-up](../xpsb-re/unknowns.md) |
| P7B-U02 | What is the complete 3D/TA path from GL draw to `drmCommandWrite`? | P7E resolved private finalization and index-0 submit; P7G-010 subsequently closed the historical DrawElements entry. Exact generated state/program/scene bytes, synchronization and matching kernel behavior remain UNKNOWN; see [frozen draw](frozen-draw-closure.md) and [current decision](frozen-draw-static-decision.md). |
| P7B-U03 | Which exact SGX535 revision did the embedded USC/PDS generator target? | explicit compiler target configuration or recoverable feature table tied to build identity |
| P7B-U04 | Is any static GPU program or microkernel payload embedded here? | bounded table with size, XREF and confirmed upload/execute path; absence cannot be inferred from string search |
| P7B-U05 | How does this historical DRI stack initialize firmware, clocks and SGX state? | exact paired X-server/kernel userspace initialization path and vendor contract |

None of these findings changes Phase 7.0 or Gate B: both identification registers remain **not** SAFE-CANDIDATE; whitelist `[]`. The legacy userspace submission path is documentation, not an approved test vector.

## Bounded pre-implementation pass (P7F)

P7B-U02 is further constrained, not closed: `0x00029251` dispatches scene dirty bits into the selected eight-position setup branch, whose CPU-side vertex/index copies, two USE/USSE instruction slots and selected TA literals are now recorded [byte by byte](bounded-path-closure.md). P7F-010 also resolves `0x0002d124` as a dirty-state atom callback with a matching generic atom walker `0x0004f3ef`; context creation installs the 17-entry atom list (P7F-011), while P7G-010 subsequently closes the historical DrawElements entry. Valid shader/state inputs, PDS/USSE semantics, broader scene state, relocations and sync still block a one-draw specification. P7B-U01/U03/U05 remain UNKNOWN. P7B-U04 is unchanged: these generated bytes are not a confirmed static firmware/microkernel payload.

The earlier description of `0x00030ffd`/`0x00030fb7` as a PDS instruction buffer was incorrect. `psb_use.c`/`use_next_inst` assertions identify their eight-byte slots as USE/USSE instructions (P7F-003). `0x00040355` separately emits a PDS-related vertex output block. This changes the classification of that **buffer**, not the physical SGX revision, safety gate, or the requirement for a complete program contract. See the ranked [remaining gaps](preimplementation-decision.md).


P7G closes the historical DrawElements → private-renderer entry of P7B-U02 and removes the apparent second terminal word. Its [frozen-draw blockers](frozen-draw-blockers.md) keep fragment compiler output, scene records and bootstrap UNKNOWN. U01–U06 and Gate B are unchanged.

The [P7H compiler checkpoint](frozen-fragment-compiler-checkpoint.md) partially constrains FG-01/P7B-U02 with a two-record UniFlex input and locates the post-lowering instruction-count producer. It does not establish the emitted USSE program, link metadata or scene pixel state. The selected compiler IR after lowering remains a static reverse-engineering target, not a hardware-observation requirement.

The same trace now identifies the ordinary scalar lowerer and four selected color-component visits (P7H-004); later optimization, register assignment and encoding still determine the actual output. Candidate-family XHW init fields and operation mapping are narrowed separately (P7H-005), without establishing the full service lifetime or exact built pair. The [current frozen-draw decision](frozen-draw-static-decision.md) retains Result B and classifies each remaining edge by source and recoverability.
